import styled from 'styled-components/native';
import { metrics } from '../../styles';

export const Divider = styled.View`
  margin-top: ${metrics.base * 1.5}px;
  margin-bottom: ${metrics.base * 1.5}px;
`;
